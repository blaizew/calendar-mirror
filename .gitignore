# Real config (identifying calendar addresses) — only config.sample.json is committed
config.json

# Run logs + heartbeat (runtime state)
sync.log
launchd.out.log
launchd.err.log
menubar.out.log
menubar.err.log
state.json

# Swift build output
menubar/.build/

# Rendered LaunchAgent (contains absolute home path) — only the template is committed
*.plist

# gws stray artifact on empty/204 responses
download.html
